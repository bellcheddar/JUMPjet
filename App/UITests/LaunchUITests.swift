import XCTest

/// Interface tests, which need a simulator and therefore do not run in the
/// host-side suite.
final class LaunchUITests: XCTestCase {
    func testPlaceholder() {
        XCTAssertTrue(true)
    }
}
