
__pycache__/
.build/
.DS_Store
.swiftpm/
*.dSYM
*.dSYM.zip
*.hmap
*.ipa
*.jsonl
*.mlmodelc
*.moved-aside
*.pyc
*.xccheckout
*.xcscmblueprint
*.xcuserstate
# Capture scratch: DerivedData must live OUTSIDE this iCloud-synced folder or
# codesign fails with "resource fork, Finder information, or similar detritus".
# Compiled Core ML, derived from the .mlpackage by `xcrun coremlcompiler`.
# compute_centroids.py, and its own AlphaFold API call.
# Core ML intermediates (Phase 2)
# Download cache for the centroid reference structures. Reproducible from
# Fetched structure cache used during development
# macOS
# Session transcripts and scratch (never commit these)
# Swift Package Manager
# Xcode
build/
DerivedData/
Fixtures/cache/
Fixtures/centroid-cache/
Models/*.mlmodelc/
Packages/*/.build/
Packages/*/.swiftpm/
scratch/
scratchpad/
Tools/coreml/.venv/
xcuserdata/

# Carries the Apple team id; generated by Tools/configure-signing.rb.
Config/Signing.xcconfig

# Generated by Tools/appstore/export-options.sh; carries the team id.
build/ExportOptions.plist
